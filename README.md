# remove-google-tl
A lightweight Chrome extension that prevents Google Search from forcing translated search results, restoring original page titles and stripping translation parameters (like ?tl=pt-br) from URLs.
